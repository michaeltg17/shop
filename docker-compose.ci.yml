services:
  ci:
    build:
      context: .
      dockerfile: Dockerfile.ci
    image: angular-ci:latest
    environment:
      - DISPLAY=:99
      - NODE_ENV=ci
      - SONAR_TOKEN
    volumes:
      - ./coverage:/app/coverage
      - ./reports:/app/reports
    command: ["/bin/bash", "/app/run-ci.sh"]